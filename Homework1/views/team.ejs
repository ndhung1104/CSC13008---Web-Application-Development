<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Team</title>

  <!-- Bootstrap CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS tuỳ chỉnh -->
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="team-page">

<section class="py-5">
  <div class="container">
    <div class="row align-items-center">
      <!-- left: cards grid (on large screen it appears left because order classes) -->
      <div class="col-lg-6 order-2 order-lg-1">
        <div class="row g-3">
          <% team.forEach(function(member){ %>
            <%- include('partials/member-card', { member: member }) %>
          <% }); %>
        </div>
      </div>

      <!-- right: giới thiệu text -->
      <div class="col-lg-6 order-1 order-lg-2">
        <div class="ms-lg-5 mb-5">
          <span class="text-muted">Nhóm</span>
            <h2 class="display-5 fw-bold mb-3 mb-xl-5">Thành viên nhóm</h2>
            <p class="lead">
            Nhóm của chúng tôi là một tập thể đầy năng lượng, sáng tạo và gắn kết, luôn phối hợp nhịp nhàng từ ý tưởng, thiết kế đến triển khai.
            </p>
            <p class="lead">
            Mỗi thành viên đều có tinh thần trách nhiệm cao, luôn nhất trí trong mọi quyết định, không bao giờ trễ deadline với tỉ lệ tham gia họp nhóm luôn đạt 100%.
            </p>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- Bootstrap JS bundle (optional) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
